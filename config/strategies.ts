/**
 * 策略版本管理系统
 * 用于管理不同版本的 Prompt、模型配置、工具使用等
 */

export interface Strategy {
  id: string;
  name: string;
  description: string;
  model: string;
  useGoogleSearch: boolean;
  prompt: string;
}

/**
 * 所有可用的策略版本
 */
export const STRATEGIES: Record<string, Strategy> = {
  'v1-baseline': {
    id: 'v1-baseline',
    name: 'Baseline v1.0',
    description: '基线版本：简单prompt，无搜索功能（54/100分）',
    model: 'gemini-2.5-flash',
    useGoogleSearch: false,
    prompt: `
      分析这张截图，提取其中的关键信息。

      要求：
      1. 用中文输出
      2. 提炼核心观点，不要只是复述文字
      3. 如果有多个话题，分成多张卡片
      4. 标题要吸引人，内容要通俗易懂
    `
  },

  'v2-with-search': {
    id: 'v2-with-search',
    name: 'v2.0 with Google Search',
    description: '优化版本：实体拆分 + Google搜索 + 增量信息（目标70+分）',
    model: 'gemini-2.5-flash',
    useGoogleSearch: true,
    prompt: `
你是一个知识卡片生成助手。分析截图并生成高质量的知识卡片。

**当前时间**：2025年12月4日

## 📋 核心要求

### 1. 识别主要实体
- 识别截图中的**主要实体**（报告、文章、产品等）
- 通常生成 1-2 张卡片，聚焦用户真正关心的内容
- 不要强行拆分无关紧要的信息

### 2. 搜索验证信息（必须执行）
**你必须使用搜索功能**查找并验证：
- ✅ 准确的名称
- ✅ 真实的发布时间（注意：如果截图显示"今天"、"最近"，结合当前时间2025年12月推断）
- ✅ 可访问的官方链接（**禁止编造链接**）
- ✅ 核心内容（非截图中的）
- ✅ 原文金句（需要中英文对照）

### 3. 生成卡片内容

**标题**：
- 简短有吸引力（最多15字）
- 不要直接复制截图文字

**摘要**：
- 一句话总结核心价值

**正文内容**（markdown格式）：
- **禁止重复截图中的信息**
- 提供增量价值：背景、关键发现、实用价值
- **只提供你通过搜索验证过的链接**
- 标注信息来源

**金句格式**（如果有）：
```
**金句**：
> "原文英文金句"
>
> 「中文翻译」

来源：[具体来源]
```

### 4. 严格验证

生成内容前自检：
- □ 发布时间是否合理？（2025年12月之前）
- □ 链接是否真实存在？（**如果搜索没找到，不要编造**）
- □ 信息是否通过搜索验证？
- □ 金句是否提供了中英文对照？

## ⚠️ 严格禁止

- ❌ **禁止编造链接** - 如果搜索没找到，写"暂无官方链接"
- ❌ **禁止编造数据** - 如果不确定，明确说明"未找到相关信息"
- ❌ 不要重复截图中已有的信息
- ❌ 不要给出错误的发布时间
- ❌ 英文金句必须附带中文翻译

## 📝 示例

**截图内容**："今天看到 Anthropic 发布新报告"

✅ **正确输出**：

**标题**：Anthropic 最新提示词工程报告

**摘要**：系统性总结了 prompt 工程的核心技术与最佳实践

**正文**：
根据搜索结果，Anthropic 于 2025年12月发布了《Prompt Engineering Guide》，全面介绍了提示词工程的关键技术...

**官方资源**：
- 如果找到链接：[查看完整报告](真实URL)
- 如果没找到：暂无官方链接，建议访问 Anthropic 官网查询

**核心观点**：
1. Few-shot learning 能显著提升准确率
2. Chain-of-thought 推理适用于复杂任务
3. ...

**金句**：
> "Well-designed prompts can improve model accuracy by up to 50%"
>
> 「精心设计的提示词可以将模型准确率提升最多50%」

来源：报告第12页

---

**输出原则**：真实性 > 完整性。宁可说"未找到"，不要编造。
    `
  }
};

/**
 * 默认策略
 */
export const DEFAULT_STRATEGY_ID = 'v1-baseline';

/**
 * 获取策略配置
 */
export function getStrategy(strategyId: string = DEFAULT_STRATEGY_ID): Strategy {
  const strategy = STRATEGIES[strategyId];
  if (!strategy) {
    throw new Error(`Strategy not found: ${strategyId}`);
  }
  return strategy;
}

/**
 * 获取所有策略列表
 */
export function getAllStrategies(): Strategy[] {
  return Object.values(STRATEGIES);
}

# Prompt优化方案 v2.0 - 基于首次评估反馈

**创建时间**: 2025-12-04
**基线分数**: 54/100
**目标分数**: 70+ /100

---

## 📊 问题诊断（基于真实反馈）

### 问题1：实体识别与卡片拆分 ⚠️ HIGH

**现状**：
```
截图包含2个实体：
1. Anthropic报告（主要）
2. Vibe Coding PPT工具（次要）

AI输出：1张混合卡片
```

**问题**：
- 两个独立主题被混在一张卡片里
- 用户真正关心的是Anthropic报告，但被PPT工具干扰

**用户反馈**：
> "我们明显应该把这两个主题分散到不同的卡片里"

**解决方案**：
```
Prompt中添加：
- 明确指示：一个实体 = 一张卡片
- 如果截图提到多个独立的事物（报告、工具、产品等），分别创建卡片
- 示例：一个视频介绍了3个产品 → 生成3张卡片
```

---

### 问题2：增量价值缺失 - 信息重复 🚨 CRITICAL

**现状**：
```
截图内容：
- "2万字报告"
- "提效50%"

AI输出：
- "2万字的报告"  ← 原文照抄
- "效率提升50%"   ← 原文照抄
```

**问题**：
- 大量重复截图中已有的信息
- 没有提供真正的增量价值
- 只是对截图做二次加工

**用户反馈**：
> "你可以看到我们提炼出来的内容和截图里的内容有大量的重复...这是完全不合格的"

**解决方案**：
```
1. 明确禁止重复截图内容
2. 要求提供"截图中没有的信息"
3. 强制要求搜索外部信息
```

---

### 问题3：缺少搜索能力 🚨 CRITICAL

**现状**：
```
用户需要：
- 报告叫什么名字？
- 报告在哪里看？
- 报告讲了什么？
- 有什么金句？

AI输出：
- ❌ 没有报告名称
- ❌ 没有链接
- ❌ 没有金句
- ❌ 只是基于截图猜测
```

**问题**：
- 当前Prompt没有要求AI搜索
- AI只能基于截图编造内容
- 无法提供可验证的信息

**用户反馈**：
> "我们非常关键的一环是，我们必须要具备搜索功能。我们要去帮用户找到这个报告是什么，讲了什么内容，而不是对这短短的截图内容做二次加工，那产生不了什么新的价值"

**解决方案**：
```
Prompt中添加搜索指令：

1. 识别截图中提到的实体（报告、文章、产品、工具等）
2. 使用搜索功能查找：
   - 准确名称
   - 发布时间
   - 官方链接
   - 核心内容
   - 原文引用
3. 输出时必须包含来源和链接
```

---

## 🎯 优化策略

### 策略1：添加搜索能力（最优先）

**当前Gemini 2.5 Flash支持的功能**：
- ✅ 内置Google Search grounding
- ✅ 可以联网查找信息
- ✅ 可以引用来源

**实现方式**：
```typescript
// 在 api/process.ts 中添加 grounding 配置
const response = await ai.models.generateContent({
  model: 'gemini-2.5-flash',
  contents: [
    {
      role: 'user',
      parts: [
        { inlineData: { mimeType: 'image/jpeg', data: base64Image } },
        { text: prompt }
      ]
    }
  ],
  generationConfig: {
    responseMimeType: 'application/json',
    responseSchema: responseSchema,
  },
  // 🔥 关键：添加 Google Search grounding
  config: {
    groundingConfig: {
      groundingSearch: {
        dynamicRetrievalConfig: {
          mode: 'MODE_DYNAMIC', // 自动决定是否搜索
          dynamicThreshold: 0.3  // 搜索阈值
        }
      }
    }
  }
});
```

**Prompt指令**：
```
如果截图中提到了具体的实体（报告、文章、产品、工具等），你必须：

1. 使用搜索功能查找该实体的准确信息
2. 提供以下内容：
   - 准确的名称
   - 发布时间/版本
   - 官方链接或权威来源
   - 核心内容（非截图中的）
   - 如果是文章/报告，提供1-2句原文金句

3. 在content中标注信息来源，例如：
   - "根据官网信息..."
   - "报告原文指出..."
   - 使用markdown链接：[报告名称](https://...)

⚠️ 禁止：
- 不要重复截图中已有的信息
- 不要编造数据
- 不要猜测
- 如果搜索不到，明确说明"未找到相关信息"
```

---

### 策略2：实体拆分指令

**Prompt指令**：
```
如果截图中包含多个独立的实体或主题，为每个实体创建独立的卡片。

示例：
- 视频介绍了3个AI工具 → 生成3张卡片
- 文章提到2篇论文 → 生成2张卡片
- 截图同时有产品介绍和使用教程 → 生成2张卡片

每张卡片聚焦一个明确的主题，不要混合。
```

---

### 策略3：增量价值自检

**Prompt指令**：
```
输出前自检清单：

□ 是否提供了截图中没有的新信息？
□ 是否有具体的时间、地点、人物、数据？
□ 是否有可验证的来源（链接、引用）？
□ 是否重复了截图中的内容？（如果是，删除）
□ 如果提到数据，是否标注了来源？

如果以上任一项不通过，重新生成内容。
```

---

## 📝 新Prompt草案

### 完整Prompt v2.0

```
你是一个智能知识卡片生成助手。你的任务是分析截图，为用户生成有价值的知识卡片。

## 核心原则：增量价值

用户截图的目的是**保存感兴趣的信息，但不想自己去深入查找**。你的价值在于：
1. 帮用户找到截图中提到的具体内容（报告、文章、产品等）
2. 提供截图中**没有的**关键信息
3. 节省用户自己搜索和阅读的时间

## 工作流程

### 第1步：识别截图中的实体

识别截图中提到的具体实体，例如：
- 研究报告、论文、文章
- 产品、工具、服务
- 概念、方法论
- 人物、公司、组织

如果有多个独立实体，为每个实体创建独立的卡片。

### 第2步：搜索外部信息（关键！）

对于每个识别出的实体，使用搜索功能查找：
- ✅ 准确的名称
- ✅ 发布时间/版本
- ✅ 官方链接或权威来源
- ✅ 核心内容（必须是截图中没有的）
- ✅ 原文引用（如果是文章/报告）

### 第3步：生成卡片内容

**内容要求**：
1. **标题**：简洁有力，吸引人（不超过15字）
2. **摘要**：一句话TL;DR
3. **正文**：
   - 提供搜索到的关键信息
   - 使用markdown格式（标题、列表、引用、链接）
   - 标注信息来源
   - 如果是报告/文章，包含1-2句原文金句（使用引用格式 > "..."）

**示例结构**：
```markdown
**报告名称**: [准确名称](链接)
**发布时间**: YYYY-MM

**核心发现**:
- 发现1（来自报告原文）
- 发现2
- 发现3

**原文金句**:
> "这是报告中的原话..."

**为什么重要**: [简短说明]
```

### 第4步：自检

输出前确认：
- [ ] 是否提供了截图中没有的新信息？
- [ ] 是否重复了截图内的内容？（如果是，删除）
- [ ] 是否有可验证的来源？
- [ ] 如果有多个实体，是否分成了独立的卡片？

## ⚠️ 禁止行为

1. ❌ 重复截图中已有的信息（数字、描述等）
2. ❌ 编造数据或猜测内容
3. ❌ 混合多个独立主题到一张卡片
4. ❌ 提供无法验证的信息

## 输出格式

按照以下JSON schema输出（已在代码中定义）。

现在，请分析这张截图：
```

---

## 🧪 测试计划

### 测试1：基线对比

```bash
# v1.0 (当前简化版)
总分: 54/100
- 需求预测: 12/20
- 需求满足: 18/50
- 表现力: 24/30

# v2.0 (添加搜索 + 实体拆分)
目标: 70+/100
- 需求预测: 16+/20 (准确识别用户需求)
- 需求满足: 40+/50 (提供搜索到的真实信息)
- 表现力: 24/30 (保持)
```

### 测试2：关键指标

| 指标 | v1.0 | v2.0目标 |
|------|------|----------|
| 报告名称 | ❌ 无 | ✅ 准确 |
| 发布时间 | ❌ 无 | ✅ 有 |
| 官方链接 | ❌ 无 | ✅ 有 |
| 原文金句 | ❌ 无 | ✅ 1-2句 |
| 卡片数量 | 1张混合 | 2张独立 |
| 信息重复率 | 高（>50%） | 低（<20%） |

---

## 🚀 实施步骤

### Step 1: 更新 api/process.ts

1. 添加 Google Search grounding 配置
2. 替换简化版 prompt 为 v2.0 完整版
3. 测试 grounding 是否正常工作

### Step 2: 部署到 Vercel

```bash
git add api/process.ts
git commit -m "feat: Add search grounding and optimized prompt v2.0"
git push
```

### Step 3: 重新测试 case_001

```bash
# 上传同一张图片，获取新的AI输出
npm run evaluate-json case_001 <new-output.json> <gemini-key>
```

### Step 4: 对比结果

```bash
# 对比 v1.0 vs v2.0
- 是否分成了2张卡片？
- 是否有报告名称、时间、链接？
- 是否有原文金句？
- 是否还重复截图内容？
```

---

## 💡 预期效果

### 用户体验改进

**Before (v1.0)**:
```
用户：看到小红书推荐Anthropic报告
↓
截图 → AI分析 → 得到一张卡片
↓
内容：重复截图内容（2万字、50%），没有报告名称和链接
↓
用户：还是要自己去搜 😞
```

**After (v2.0)**:
```
用户：看到小红书推荐Anthropic报告
↓
截图 → AI分析（自动搜索）→ 得到2张卡片
↓
卡片1 - Anthropic报告：
  - 报告名称：《How Anthropic Uses AI》
  - 发布时间：2024-11
  - 链接：https://...
  - 核心观点：（来自报告原文）
  - 金句："We've found..."

卡片2 - Vibe Coding工具（次要）
↓
用户：太棒了，不用自己搜了！ 🎉
```

---

## 📊 成功指标

1. **评估分数**: 从54分提升到70+分
2. **用户满意度**: 从"不满意"到"满意"
3. **增量价值**: 从"信息重复率>50%"到"<20%"
4. **完整性**: 必备信息（名称、时间、链接、金句）100%覆盖

---

## 🔄 迭代计划

### v2.0: 搜索能力 + 实体拆分（当前）
- 添加 Google Search grounding
- 优化 prompt 指令
- 测试基线case

### v2.1: Few-Shot示例（如果v2.0效果不够）
- 添加好/坏案例对比
- 强化增量价值理解

### v2.2: 用户反馈迭代（基于v2.0测试结果）
- 收集新的用户反馈
- 针对性优化

---

**准备好实施了吗？**

下一步：
1. 实现 api/process.ts 的 grounding 配置
2. 更新 prompt 为 v2.0
3. 部署并重新测试
